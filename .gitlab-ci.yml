stages:
  - build
  - test

variables:
  MAVEN_OPTS: "-Dmaven.repo.local=.m2/repository"

cache:
  paths:
    - .m2/repository/
    - target/

build:
  stage: build
  script:
    - mvn compile

smokesuite:
  image: markhobson/maven-chrome:jdk-11
  stage: test
  script:
    - mvn test -Dsuite=smoke -Denvironment=qa

regressionsuite:
  image: markhobson/maven-chrome:jdk-11
  stage: test
  script:
    - mvn test -Dsuite=regression -Denvironment=stage

fullsuite:
  image: markhobson/maven-chrome:jdk-11
  stage: test
  script:
    - mvn test -Dsuite=full -Denvironment=stage